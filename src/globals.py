import flet as ft

class Globals:
    first_time = True
    music_dir = ""
    # Themes
    red_dark_default = ft.Theme(color_scheme_seed=ft.Colors.RED)
    purple_dark_default = ft.Theme(color_scheme_seed=ft.Colors.PURPLE)
    green_dark_default = ft.Theme(color_scheme_seed=ft.Colors.GREEN)